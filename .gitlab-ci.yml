stages:
  - check
  - build_api
  - build_front

lint_code:
  stage: check
  image: python:3.10-slim
  before_script:
    - python3 ./api/test_main.py
    - pip install flake8
  script:
    - flake8 . --exclude=venv

build_api:
  stage: build_api
  image: docker:29
  services:
    - docker:29-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - echo "$CI_JOB_TOKEN" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - docker build -t "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHA" ./api
    - docker push "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHA"

build_front:
  stage: build_front
  image: docker:29
  services:
    - docker:29-dind
  variables:
    DOCKER_TLS_CERTDIR: "/certs"
  before_script:
    - echo "$CI_JOB_TOKEN" | docker login -u "$CI_REGISTRY_USER" --password-stdin "$CI_REGISTRY"
  script:
    - docker build -t "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHA" ./front-end-nextjs
    - docker push "$CI_REGISTRY_IMAGE:$CI_COMMIT_SHA"
